# Python 3.13 対応 LLM Chat System 必要パッケージ
# 基本 langchain パッケージ: 現在使用している拡張 (langchain-groq / langchain-openai) は
# langchain-core < 1.0.0 系を要求するため、互換性のために 0.3.x 系に固定します。
# langchain-groq は >=0.3.75, langchain-openai は >=0.3.78 を要求するため、
# 両方を満たす最新の 0.3.x バージョンを使用します。
langchain==0.3.27
langchain-core>=0.3.75,<1.0.0
langgraph>=1.0.1

# OpenAI
langchain-openai

# Anthropic (Claude)
langchain-anthropic

# Google (Gemini)
langchain-google-genai

# Grok
langchain-xai

# Groq
langchain-groq

# Mistral
langchain_mistralai

# Together AI
langchain-together

# Web検索機能 (ChatSearch.py, langgraph_web_search.py)
requests>=2.31.0
beautifulsoup4>=4.12.0

# 一般的なパッケージ
pydantic>=2.7.4
typing-extensions>=4.7.0

# オプション: AI プロバイダー固有パッケージ
# openai  # OpenAI API直接利用時
# anthropic  # Anthropic API直接利用時
# google-generativeai  # Google Gemini API直接利用時
